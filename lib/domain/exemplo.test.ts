import { describe, it, expect } from "vitest";

// Teste de fumaça — confirma que o Vitest está configurado.
// Apague quando o primeiro teste de verdade (prazo.test.ts) existir.
describe("setup", () => {
  it("vitest funciona", () => {
    expect(1 + 1).toBe(2);
  });
});
