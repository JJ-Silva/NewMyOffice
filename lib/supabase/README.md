# lib/supabase — clients e sessão

- `server.ts` — client para Server Components / Server Actions (cookies do usuário).
- `browser.ts` — client para Client Components.
- `sessao.ts` — helpers: usuário logado, `escritorio_id` ativo, `podeFazer(membro, acao)`.

Criado no Passo 3 do `INICIO-AQUI.md` (auth + onboarding).
